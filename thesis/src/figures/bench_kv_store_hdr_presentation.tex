\begin{tikzpicture}
\begin{axis}[
    axis on top, ymin=0,
    legend pos=north east,
    legend style={
        anchor=north west,xshift=15pt,
    },
    grid=major,
    thick,
    xlabel={Set Operations (count)},
    ylabel={Median Runtime (ms)},
    sharp plot,
]
\addplot+[mark=*] table [
    x=base,
    y=median_point_estimate,
    col sep=semicolon,
] {data/bench_kv_store_hdr_nocb.csv};
\addlegendentryexpanded{Key-Value Store (\ref{code:mvr-store-datalog-dialect})}

\addplot+[mark=+] table [
    x=base,
    y=median_point_estimate,
    col sep=semicolon,
] {data/bench_kv_store_hdr_cb.csv};
\addlegendentryexpanded{With Causal Broadcast (Appendix \ref{code:mvr-crdt-datalog-dialect})}
\end{axis}
\end{tikzpicture}
